# Perplexity Pro API Configuration
# SECURITY WARNING: Never commit your actual API key to version control!
# This file is a template. Create a .env file with your actual credentials.

# Perplexity Pro API Key
# Get your API key from: https://www.perplexity.ai/settings/api
# Keep this key confidential and never share it publicly
PERPLEXITY_API_KEY=your_perplexity_api_key_here

# API Configuration
# API URL endpoint (usually doesn't need to change)
PERPLEXITY_API_URL=https://api.perplexity.ai/chat/completions

# Model Selection
# Options: pplx-70b-online (with web search), pplx-70b-chat (without web search)
PERPLEXITY_MODEL=pplx-70b-online

# Application Environment
# Options: development, production
ENVIRONMENT=development

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Application Settings
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=False

# Request Timeout (in seconds)
REQUEST_TIMEOUT=30

# Rate Limiting (optional)
# MAX_REQUESTS_PER_MINUTE=60

# IMPORTANT SECURITY NOTES:
# 1. Create a .env file (not .env.example) with your real API key
# 2. Add .env to .gitignore to prevent accidental commits
# 3. Use a secrets manager in production (AWS Secrets Manager, HashiCorp Vault, etc.)
# 4. Rotate your API keys regularly
# 5. Monitor API usage for suspicious activity
